function useDeepSpeech() {
  const transcript = '';
  const finalTranscript = '';
  const listening = false;

  const startRecording = () => {};
  const stopRecording = () => {};
  const resetSpeech = () => {};

  return {
    transcript,
    finalTranscript,
    listening,
    startRecording,
    stopRecording,
    resetSpeech,
  };
}

export default useDeepSpeech;
